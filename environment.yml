name: word2vec
channels:
  - conda-forge
dependencies:
  - python=3.7
  - pip

  # Base conda deps
  - pylint
  - flake8
  - black
  - isort
  - twine
  - wheel
  - pytest
  - pytest-cov

  # This project conda deps
  - numpy
  - cython
  - scikit-learn
  - notebook
  - jupyterlab

  - pip:
    # This project pip deps
